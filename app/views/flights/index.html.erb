<section class="section">
    <div class="container">
        <%= render 'feed', form_objects: @airports %>
    </div>
    
    <% unless @flights==nil%>
        <div class="container">
            <%= render 'consult', form_consults:@flights, count_person:@passenger_count,
            form_model:@booking %>
        </div>
    <% end %>
</section>